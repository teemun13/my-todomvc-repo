{
  "name": "todomvc-browser-tests",
  "description": "An automated test suite for TodoMVC",
  "private": true,
  "devDependencies": {
    "chromedriver": "^2.18.0",
    "mocha": "*",
    "mocha-known-issues-reporter": "git+https://github.com/ColinEberhardt/mocha-known-issues-reporter.git#v0.0.0",
    "optimist": "^0.6.1",
    "selenium-webdriver": "~2.53.1",
    "drool": "0.4.0",
    "nyc": "^15.1.0"
  },
  "scripts": {
    "server": "gulp test-server",
  "cy:run": "cypress run --env coverage=true",
  "test": "npm run server & npm run cy:run"
  }
}

